{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run Realations backend",
			"type": "shell",
			"command": "cd backend && .venv/bin/alembic upgrade head && .venv/bin/uvicorn app.main:app --reload",
			"isBackground": true
		}
	]
}