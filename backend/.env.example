# Copy to `.env` and adjust before running locally.
REALATIONS_ENVIRONMENT=dev
REALATIONS_DEBUG=true
REALATIONS_DATABASE_URL=postgresql+psycopg://realations:realations@localhost:5432/realations
REALATIONS_JWT_SECRET=replace-with-a-long-random-secret
REALATIONS_ACCESS_TOKEN_TTL_MINUTES=720
